export enum AppRoutes {
  // Auth routes
  LOGIN = '/auth/login',
  REGISTER = '/auth/register',
  
  // Main routes
  HOME = '/home',
  PROJECTS = '/projects',
  USERS = '/users',
  EVENTS = '/events',
  SEEDBEDS = '/seedbeds',
  ACTIVITIES = '/activities',
  REPORTS = '/reports',
  
  // Settings routes
  SETTINGS = '/settings',
  PROFILE = '/profile'
} 